<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nuwa.infrastructure.attract.database.reward.mapper.RewardMapper">

    <select id="selectReawdData" resultType="java.lang.String">
        SELECT
          SUM(total_reward) AS totalBonus,
          COUNT(*) AS applyNum,
          (SELECT
            COUNT(*)
          FROM
            reward
          WHERE review_status = 1) AS passNum
        FROM
          reward
        WHERE DATE_FORMAT(create_time,'%Y') = #{date};
    </select>
</mapper>
