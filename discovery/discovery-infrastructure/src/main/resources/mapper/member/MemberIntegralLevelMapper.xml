<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nuwa.infrastructure.discovery.database.member.mapper.MemberIntegralLevelMapper">


    <update id="incrCount">
        UPDATE member_integral_level SET count = count + 1 WHERE id = #{id}
    </update>

    <update id="decrCount">
        UPDATE member_integral_level SET count = count - 1 WHERE id = #{id}
    </update>
</mapper>
