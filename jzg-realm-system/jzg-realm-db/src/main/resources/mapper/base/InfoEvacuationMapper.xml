<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzy.db.dao.base.InfoEvacuationMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zzy.db.entity.base.InfoEvacuation">
        <id column="id" property="id" />
        <result column="create_time" property="createTime" />
        <result column="create_id" property="createId" />
        <result column="create_name" property="createName" />
        <result column="evacuationpicture_address" property="evacuationpictureAddress" />
        <result column="evacuation_description" property="evacuationDescription" />
        <result column="scenic_name" property="scenicName" />
        <result column="is_delete" property="isDelete" />
        <result column="release_place" property="releasePlace" />
        <result column="use_place" property="usePlace" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, create_time, create_id, create_name, evacuationpicture_address, evacuation_description, scenic_name, is_delete, release_place, use_place
    </sql>

    <select id="selectPageListInfoEvacuation" resultMap="BaseResultMap">
        select * from info_evacuation
        <where>
            <if test="departName!=null ">
                and scenic_name like Concat('%',#{departName},'%')
            </if>
        </where>
        order by create_time desc
    </select>

</mapper>
